# Environment variables for docker-compose local/dev runs
# Copy this file to `.env` at the repo root before running:
#   cp .env.example .env
# and then fill in real credentials.

# Razorpay credentials (required for SaaS server when using Razorpay)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret
