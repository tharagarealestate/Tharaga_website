# Ensure /login_signup (no-trailing) serves the folder without issuing a redirect (preserves fragment)
# and any nested routes under /login_signup map to the index for that folder.
# Use explicit 200 rewrites — DO NOT use 301/302 here.
# Specific rules first, then SPA fallback.

# 1) If someone hits /login_signup (no trailing slash) => serve /login_signup/ (200 rewrite)
 /login_signup       /login_signup/          200

# 2) Any deeper route under /login_signup should serve the login_signup index (200)
 /login_signup/*     /login_signup/index.html 200

# 3) SPA fallback for everything else
 /*                  /index.html             200
